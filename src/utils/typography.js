import Typography from 'typography';

export default new Typography({
    baseFontSize: '18px',
    baseLineHeight: 1.45,
    headerFontFamily: ['Asul', 'serif'],
    bodyFontFamily: ['Chivo', 'sans-serif']
});
